---
import BaseLayout from '../layouts/BaseLayout.astro';
import ScrollableProjectImage from '../components/ui/ScrollableProjectImage.astro';
import { portfolioItems } from '../data/site';
---

<BaseLayout
  title="Our Work | High-Converting Websites by SoCal Web Experts"
  description="See real examples of fast, modern websites built to convert leads for local service businesses."
>
  <section class="py-16 md:py-24">
    <div class="container">
      <div class="max-w-2xl mb-10 fade-in">
        <p class="text-primary-500 text-sm font-medium tracking-wide uppercase mb-3">
          Portfolio
        </p>
        <h1 class="text-3xl md:text-4xl font-bold text-[var(--text)] mb-4">
          Work built for leads and trust
        </h1>
        <p class="text-[var(--text-secondary)] text-lg">
          Modern design, clear messaging, and fast performance so customers choose you.
        </p>
      </div>

      <!-- Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5 md:gap-6">
        {portfolioItems.map((item, index) => (
          <div
            class="group fade-in"
            style={`animation-delay: ${index * 0.05}s`}
          >
            <div class="rounded-xl border border-[var(--border)] overflow-hidden transition-all duration-300 hover:-translate-y-1 hover:border-primary-500/40 hover:shadow-lg hover:shadow-black/10">
              <!-- Image -->
              <div class="aspect-[16/10] overflow-hidden bg-[var(--bg-tertiary)] flex items-center justify-center">
                {item.scrollableImage ? (
                  <ScrollableProjectImage src={item.image} alt={item.title} />
                ) : (
                  <img
                    src={item.image}
                    alt={item.title}
                    class="w-full h-full object-contain group-hover:scale-[1.03] transition-transform duration-500"
                    loading="lazy"
                  />
                )}
              </div>

              <!-- Info -->
              <div class="px-4 py-3 bg-[var(--bg-secondary)]">
                <h3 class="text-sm font-medium text-[var(--text)]">{item.title}</h3>
                <p class="text-xs text-[var(--text-secondary)] mt-0.5">{item.categoryLabel}</p>
              </div>
            </div>
          </div>
        ))}
      </div>

      <div class="my-10">
        <a href="/contact" class="inline-flex items-center justify-center px-5 py-2.5 rounded-lg bg-primary-500 text-white hover:bg-primary-600 transition-colors">
          Want one like this? Get a Quote
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
